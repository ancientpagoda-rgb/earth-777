name: Validate virtual BIOME4 PFT hydrology

on:
  push:
    branches:
      - agent/biome4-virtual-pft-hydrology

permissions:
  contents: write

jobs:
  validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/biome4-virtual-pft-hydrology
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Validate exact branch state
        run: |
          npm ci
          node --check src/sim/Biome4VirtualClimateForcing.js
          node --check src/sim/Biome4VirtualPftHydrology.js
          npm test
          npm run build
      - name: Remove transient validator
        run: |
          rm .github/workflows/validate-virtual-pft-hydrology.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Remove transient virtual PFT hydrology validator'
          git push origin HEAD:agent/biome4-virtual-pft-hydrology
