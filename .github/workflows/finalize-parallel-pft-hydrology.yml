name: Finalize parallel PFT hydrology

on:
  push:
    branches:
      - agent/biome4-parallel-pft-hydrology
    paths:
      - scripts/finalize-parallel-pft-hydrology.py
      - .github/workflows/finalize-parallel-pft-hydrology.yml

permissions:
  contents: write

jobs:
  finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/biome4-parallel-pft-hydrology
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - run: python3 scripts/finalize-parallel-pft-hydrology.py
      - run: npm test
      - run: npm run build
      - name: Commit finalized scientific status
        run: |
          rm -f scripts/finalize-parallel-pft-hydrology.py
          rm -f .github/workflows/finalize-parallel-pft-hydrology.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Finalize parallel PFT hydrology scientific status'
          git push origin HEAD:agent/biome4-parallel-pft-hydrology
